apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Only the namespaced pair. The cluster-scoped kinds are deliberately absent.
resources:
  - ../../../crd/bases/terasky.com_resourcepatches.yaml
  - ../../../crd/bases/terasky.com_sharedresources.yaml

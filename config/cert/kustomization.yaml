apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# A shared component rather than a file inside one overlay: kustomize refuses to reference a bare
# file outside an overlay's own directory, and both overlays need the webhook certificate.
resources:
  - cert.yaml
